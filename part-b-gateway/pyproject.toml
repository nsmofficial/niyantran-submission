[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
# Starlette 1.3 deprecates httpx inside its own TestClient. Nothing for us to fix.
filterwarnings = ["ignore:Using .httpx. with .starlette.testclient. is deprecated"]
